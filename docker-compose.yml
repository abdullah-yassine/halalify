services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: halalify
      POSTGRES_USER: halalify
      POSTGRES_PASSWORD: localdevpassword
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
